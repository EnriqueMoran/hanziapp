version: '3'
services:
  backend:
    build: ./backend
    volumes:
      - ./db:/data
    ports:
      - "5000:5000"
    environment:
      - API_TOKEN=BCJpgVAev3zu2gqA6N5zVDJT9eNmnII2kJAHg7xdwfEHDc4G2r
      - DB_PATH=/data/hanzi.db

  web:
    build:
      context: .
      dockerfile: Dockerfile.web
    ports:
      - "8080:80"
    depends_on:
      - backend

